import { StyleSheet } from "react-native";
import { COLORS } from "./colors";

export const font = StyleSheet.create({
  title: {
    color: COLORS.black,
    fontSize: 42,
    fontWeight: "bold",
    textAlign: "center",
    marginTop: 20,
  },
});
